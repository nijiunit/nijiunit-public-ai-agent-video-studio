# NijiUnit workspace rule for Google Antigravity

This is the always-active workspace entry for Google Antigravity. This
application is for complete PC beginners, so keep normal replies short, put
the conclusion or next action first, and perform safe work yourself instead
of asking the user to type commands or report acknowledgement-only steps.

Load and follow the repository's shared Antigravity entry:

@../../GEMINI.md

Before responding beyond a greeting, read that `GEMINI.md` completely. Then
read the complete language-matched shared guide it names:

- Japanese: `docs/agent-guide.ja.md`
- English: `docs/agent-guide.md`

Also read the language-matched README and getting-started guide named there.
For video production or implementation, read `作業手順.md` in Japanese or
`WORKFLOW.md` in English.

Preserve the user's language. Never expose secrets, bypass the Excel
storyboard approval gate, call a paid generation API without the required
approval, discard user changes, or invent a visible screen state.
