[English](WORKFLOW.md) | 日本語

# AI動画制作 作業手順（公開版）

最終更新日: 2026-08-06  
方式: キャラクター台帳・専用デザイン動画・3秒連続生成

## 1. 基本原則

作業の起点は`input`です。再登場キャラクターは`characters`の有効版を正とします。キャラクターは次の四層で固定します。

| 層 | 固定するもの | データ |
|---|---|---|
| 外見台帳 | 顔、形、体格、色、衣装、材質、禁止事項 | `profile.json`、正式画像 |
| 通常存在 | 瞬き、呼吸、静止姿勢、動作の重さ | `design_presence.mp4`、3キーフレーム |
| 固有動作 | 驚き、修理、挨拶など | 固有動作MP4、タイミング、3キーフレーム |
| ショット継続 | 3秒境界の位置・姿勢・光 | 直前クリップの最終フレーム |

正式なデザイン動画は、過去の完成動画の切り抜き、静止画ループ、引き伸ばし映像ではなく、承認済み外見からキャラクター専用に新規生成します。

## 2. 初回セットアップ

```powershell
py -3 -m venv .venv
.\.venv\Scripts\python.exe -m pip install -e ".[dev]"
Copy-Item .env.example .env
```

APIキーは`.env`だけに置きます。チャット、Markdown、JSON、Excel、ログ、スクリーンショットへ書きません。

## 3. キャラクター台帳を作る

`templates/character-profile.json`と`templates/character-registry.json`を複写し、`characters/<id>/<version>`へ配置します。顔や基本デザインを変えるときは既存版を上書きせず、`v002`のような新版を作ってから`active_version`を切り替えます。

公開可能なキャラクターは、次も必須です。

- `source_type`: `original`、`generated`、`third_party`のいずれか
- `asset_license`: 利用条件
- `publishable`: 公開審査済みなら`true`
- `source_notes`: 作成日、生成方法、参照元、除外した権利物

通常存在と固有動作の動画を生成し、検証します。

```powershell
.\.venv\Scripts\python.exe scripts\generate_character_design_videos.py `
  --registry-dir characters
.\.venv\Scripts\python.exe run_storyboard.py prepare-motions `
  --registry-dir characters --overwrite
.\.venv\Scripts\python.exe run_storyboard.py validate-characters `
  --registry-dir characters
.\.venv\Scripts\python.exe scripts\validate_character_design_videos.py `
  --registry-dir characters
```

正式仕様は3.00秒、1280×720、24fps、16:9、9枚の実動画確認フレームです。全フレームで外見が安定し、意図した動きが目視できることを人が確認します。

## 4. inputを準備する

最低限、`input/story.md`へ次を書きます。

- 登場人物と目的
- 起きる出来事、最後の落ち、必要なら教訓
- 誰が何を話すか
- 希望する画風と画角
- 変えてはいけない設定
- 正確に表示する文字

素材には入手元と利用条件を記録します。再登場キャラクターを、案件ごとの古い画像で上書きしません。

## 5. 3秒構成を作る

```powershell
.\.venv\Scripts\python.exe run_storyboard.py create
```

1ショットは一つの意味と一つの主動作へ絞ります。歩行、説明、操作、驚き、落ちを同じ3秒へ詰めません。台詞は実際に3秒で読める長さにします。

同じ場面が続くショットは`continuity_start_mode: previous_final_frame`、場所・時刻・構図を切り替えるショットは`storyboard_image`にします。

この段階の`storyboard.json`は生成処理用データであり、利用者が確認する正式コンテではありません。JSONやMarkdownだけを見て動画生成へ進みません。

## 6. 開始画像を確認し、Excelコンテを作る

```powershell
.\.venv\Scripts\python.exe run_storyboard.py render-images `
  --run-dir output\storyboard\v001 --limit 1
```

最初の1枚で、16:9、全員の外見、左右位置、小物、背景、文字やロゴの混入、字幕余白を確認してから残りへ進みます。誤った開始画像のまま動画を生成しません。

最初の画像が合格したら`--limit`を外して全ショットの画像を生成します。全画像が揃うと、`review/storyboard_v001.xlsx`のような正式レビュー用Excelコンテと、日本語・英語のローカルHTML確認画面が自動作成されます。明示的に再構築した場合、確認済みファイルを上書きせず`_r002`、`_r003`のような新版を作ります。

```powershell
.\.venv\Scripts\python.exe run_storyboard.py render-images `
  --run-dir output\storyboard\v001
.\.venv\Scripts\python.exe run_storyboard.py build-workbook `
  --run-dir output\storyboard\v001
```

Excelの各シートには、メイン画像、画面説明、主動作、表情、カメラ、光、台詞、音、連続性、9コマ計画、レビュー状態、黄色い訂正指示欄を入れます。全ショットのメイン画像がない状態では正式Excelを作れません。
再構築した新版の承認状態は`未確認`です。古い承認済み版が残っていても、画像や構成を変えた最新の版を必ず再確認します。

## 7. Excelコンテを確認・修正・承認する

Excelコンテが正式な人間確認用成果物です。AIエージェントは、パスやチャットのリンクを示すだけで終わらず、次のコマンドで該当フォルダを開き、成果物を青く選択して、ここで必ず停止します。

```powershell
.\.venv\Scripts\python.exe run_storyboard.py reveal-artifact `
  --run-dir output\storyboard\v001 --artifact storyboard --language ja
```

Excel、LibreOffice Calc、Numbersのいずれかが見つかればExcelコンテを選択します。表計算アプリがなければ、日本語のローカルHTML確認画面を選択します。HTMLはインターネットへ内容を送信せず、カードごとのOK・修正内容をブラウザー内へ保存できます。正式記録は同じフォルダのExcelのままです。

利用者へは、最初に「青く選択されたファイルをダブルクリックしてください。開いたら『開いた』と返してください」と一つだけ案内します。「開いた」の返答後、Excelなら全シートを確認し、問題があれば`レビュー状態`を`修正必要`にして黄色い欄へ指示を書き、保存してもらいます。HTMLなら上からカードを確認し、最後に作った確認結果をチャットへ貼り付けてもらいます。一度に複数操作を案内しません。

```powershell
.\.venv\Scripts\python.exe run_storyboard.py extract-corrections `
  --workbook output\storyboard\v001\review\storyboard_v001.xlsx
```

AIエージェントは訂正を`storyboard.json`と開始画像へ反映し、Excelを作り直して再確認を依頼します。利用者がチャットまたはExcelで明示的に承認した後だけ、次を実行します。

```powershell
.\.venv\Scripts\python.exe run_storyboard.py approve-workbook `
  --run-dir output\storyboard\v001
```

`approve-workbook`は、利用者の明示的な承認を受けたAIエージェントが未確認シートを一括で承認状態にするためのコマンドです。ただし、修正必要または未反映の訂正指示が一つでもあれば失敗します。全シートが承認状態のExcelがないと`render-videos`も実行を拒否します。AIエージェントは無言、ファイルの存在、過去版の承認から今回の承認を推測してはいけません。

Excelが開いたままで承認状態を保存できない場合は、保存して閉じる一操作だけを案内し、「閉じた」という返答を待ってから再実行します。遠隔環境などでフォルダを開けない場合は、開いたふりをせず、正確なフォルダ名とファイル名を一つずつ案内します。

## 8. 3秒動画を生成する

```powershell
.\.venv\Scripts\python.exe run_storyboard.py render-videos `
  --run-dir output\storyboard\v001
```

実装は、正式画像、通常存在、発動した固有動作、禁止事項を各ショットへ固定します。参考MP4を直接扱えないモデルには、0.25秒、1.50秒、2.75秒付近のキーフレームと3秒の動作タイミングを送ります。

同じ場面では直前クリップの最終フレームを自動抽出し、次の開始フレームへ使います。各ショットの生成映像は9コマへ分解し、人物の増殖、部品数、色、姿勢、画角、3秒境界を確認します。

## 9. 音声・音楽・字幕を仕上げる

動画APIが生成した音声は完成素材に使わず、映像ストリームだけを採用します。

- 台詞とナレーション: 専用TTSで生成し、尺と発音を確認
- 環境音と効果音: 利用条件が明確な素材またはローカル生成
- 音楽: 権利と出典を記録し、台詞を邪魔しない音量に調整
- 日本語テロップ: ASSなどを使ってローカル合成し、文字を機械検査

複数の話者がいる場合は、ショット別の`voice`、`speaker`、`style`をJSONへ記録し、`--voice-config`で渡します。環境音は3秒ごとに個別生成するだけでなく、完成尺全体を一本の時間軸として設計します。前ショットの余韻、音源への接近、画面横断に合わせた左右移動、台詞中の背景音低下、着地後の静けさまで連続させます。

「音声ストリームあり」は品質合格を意味しません。最初から最後まで再生し、知覚できる無音、口の動きと台詞の不一致、環境変化と音量変化の不一致、急な音切れを確認します。全尺のLUFSとtrue peakに加え、各3秒区間の平均・最大音量、波形、スペクトログラムも記録します。

```powershell
.\.venv\Scripts\python.exe scripts\generate_storyboard_tts.py `
  --run-dir output\storyboard\v001 `
  --voice-config examples\space-friends\tts_config.json

.\.venv\Scripts\python.exe scripts\rebuild_clean_soundtrack.py `
  --run-dir output\storyboard\v001 `
  --ambience-profile space-to-nature
```

映画的な一本の音響を作品専用に組む場合は、`build_cinematic_soundtrack.py`のようにローカルで合成します。先に字幕付き映像を連結し、その後、動画API由来の音声を一切引き継がず、映像ストリームと承認済み音声だけを明示的にmuxします。

```powershell
.\.venv\Scripts\python.exe scripts\build_cinematic_soundtrack.py `
  --run-dir output\space-friends\v002 `
  --input-video output\space-friends\v002\final\story_video_v002.mp4 `
  --output-video output\space-friends\v002\final\story_video_v002_cinematic.mp4
```

TTSは別の音声認識モデルで文字起こしし、句読点や表記差を除いた台本との一致を記録します。完成ミックスも再度文字起こしし、背景音を加えた後も台詞が認識できることを確認します。

音声のみの文字起こしを行い、台詞の欠落、混線、不要な生成音声がないことを確認します。

## 10. 連結・最終確認

```powershell
.\.venv\Scripts\python.exe run_storyboard.py finalize-video `
  --run-dir output\storyboard\v001
.\.venv\Scripts\python.exe run_storyboard.py build-video-workbook `
  --run-dir output\storyboard\v001
```

生成動画9コマ確認、完成MP4、AIモデル使用記録も、それぞれのフォルダを開き対象を選択して渡します。

```powershell
.\.venv\Scripts\python.exe run_storyboard.py reveal-artifact `
  --run-dir output\storyboard\v001 --artifact video-review --language ja
.\.venv\Scripts\python.exe run_storyboard.py reveal-artifact `
  --run-dir output\storyboard\v001 --artifact final-video --language ja
.\.venv\Scripts\python.exe run_storyboard.py reveal-artifact `
  --run-dir output\storyboard\v001 --artifact ai-record --language ja
```

人が最初から最後まで再生し、映像、音声、字幕、落ち、尺、権利表記を確認します。特に、話していないキャラクターの口やサービスシームが動いていないか、飛行中の首・頭・胴体が同じ方向か、環境音が画面内の距離と左右位置に合うかを確認します。一度確認した版は上書きせず、`v002`のような新版を作ります。不採用素材は理由の分かる名前で対象ランの`rejected`へ退避します。

## 11. AIモデル使用記録を必ず残す

1話ごとに`templates/AIモデル使用記録.md`を複写し、完成ランの`final/AIモデル使用記録.md`へ保存します。使用しなかった工程も「未使用」と記録します。

最低限、次を工程別に残します。

- 企画・脚本・絵コンテ
- キャラクター外見設計
- キャラクターデザイン動画
- 開始画像
- 3秒動画（ショット範囲ごと）
- TTS、ナレーション
- 音楽、環境音、効果音
- 音響の全体設計、台詞位置、LUFS、true peak、区間別音量
- 字幕、連結、色・音量調整
- 文字起こし、品質検査
- 人が行った最終判断

モデル名だけでなく、提供元、機能、入力、出力、採用範囲、再生成理由、ローカル処理、実施日も記録します。モデルを切り替えたショットは範囲を分けます。

## 12. 公開前検査

- `.env`、キー、メール、個人パス、案件IDがない
- `input`と`output`の制作内容がGit管理されていない
- 実在人物、顧客、家族、ペット、過去作品が混入していない
- 全素材に公開根拠とライセンスがある
- 生成サービスのoperation IDや非公開メタデータがない
- APIを使わない状態でテストが通る
- Gitへ追加される全ファイルを`git diff --cached`で確認した
