# characters

Reusable project characters live here in local use. Start from the files in
`templates/`, or inspect the publishable registry in
`examples/space-friends/characters`.

Do not commit real-person likenesses or private production characters to a
public fork. Every publishable profile must include provenance, an asset
license, and `publishable: true`.
