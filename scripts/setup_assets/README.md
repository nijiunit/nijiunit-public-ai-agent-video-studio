# NijiUnit setup assets

These NijiUnit-owned images are bundled so the loopback-only first-time setup
page does not contact an external image server. They are part of this public
repository with the owner's permission.
